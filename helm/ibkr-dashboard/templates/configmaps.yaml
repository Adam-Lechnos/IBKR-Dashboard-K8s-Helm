apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmaps.dashboard.name }}
  labels:
    {{- toYaml .Values.global.labels | default "" | nindent 10 }}
data:
  {{- toYaml .Values.configmaps.dashboard.data | default "" | nindent 10 }}