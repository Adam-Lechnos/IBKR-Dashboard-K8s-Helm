apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.dashboard.users.name }}
  labels:
    {{- toYaml .Values.global.labels | default "" | nindent 10 }}
data:
  .htpasswd: {{ .Values.htpasswd | b64enc | quote }}
